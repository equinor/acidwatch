apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: acidwatch
spec:
  environments:
    - name: dev
      build:
        from: add-radix-config
    - name: test
      build:
        from: add-radix-config
  
  components:
    - name: frontend
      ports:
        - name: http
          port: 3000
      publicPort: http
      src: frontend
