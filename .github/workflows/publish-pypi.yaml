name: Publish to PyPI

on:
  push:
    tags:
      - v*

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    environment:
      name: pypi
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: ./backend

      - name: Set up Python 3.12
        uses: actions/setup-python@v3
        with:
          python-version: "3.12"

      - name: Install uv
        run: pip install uv
      - name: Build
        run: uv build --package acidwatch
      - name: Publish
        run: uv publish
