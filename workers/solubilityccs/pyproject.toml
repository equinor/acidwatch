[project]
name = "acidwatch-worker-solubilityccs"
version = "0.1.0"
requires-python = ">=3.12, <3.14"
dependencies = [
  "acidwatch-models",
  "acidwatch-messaging",
  "solubilityccs",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
