[project]
name = "acidwatch-worker-srk-vanlaar"
version = "0.1.0"
requires-python = ">=3.12, <3.14"
dependencies = [
  "acidwatch-models",
  "acidwatch-worker-runtime",
  "neqsim",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
