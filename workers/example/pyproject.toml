[project]
name = "acidwatch-worker-example"
version = "0.1.0"
requires-python = ">=3.12, <3.14"
dependencies = ["acidwatch-models", "acidwatch-messaging"]

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
