[project]
name = "acidwatch-worker-phpitz-reactive"
version = "0.1.0"
requires-python = ">=3.12, <3.14"
dependencies = ["acidwatch-models", "acidwatch-worker-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
